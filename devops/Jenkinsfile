def runInDocker(String image, Closure body) {
   docker.image(image).inside { body() }
}
pipeline {
  // `agent any` = el pipeline se ejecuta en cualquier nodo/agente disponible.
  agent any

  stages {
    stage('Info') {
      steps {
        echo "Job: ${env.JOB_NAME}"
        echo "Build: ${env.BUILD_NUMBER}"
        echo "Branch: ${env.BRANCH_NAME}"
      }
    }
    
    stage('Test') {
      steps {
        script { 
           runInDocker('maven:3.8.6-openjdk-11-slim') {
             sh 'mvn -version'
             sh 'mvn test'
           }
        }  
      }
    }  
    /*stage('CI - Lint') {
      steps {
         runInDocker('maven:3.8.6-openjdk-11-slim') { 
            sh 'apt-get update && apt-get install -y make'
            sh 'make lint'
         }
      }
    }
    stage('CI - Test') {
      steps {
         runInDocker('maven:3.8.6-openjdk-11-slim') { 
            sh 'apt-get update && apt-get install -y make' 
            sh 'make test'
         }    
      }
    }
    /*stage('CI - Build image') {
      steps {
        script {
          docker.build('maven:3.8.6-openjdk-11-slim', "--build-arg VERSION=... -f devops/Dockerfile .")
        }
      }
    }*/
 
  }
}
